services:
  travel-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: travel-companion
    ports:
      - "3000:3000"
      - "5000:5000"
    volumes:
      - ./uploads:/app/server/uploads
      - ./db:/app/server/db/data
    environment:
      - NODE_ENV=development
      - JWT_SECRET=1lSRhkJR6s0bykhB79drwkDsArSDeRvf
      - PORT=5000
    restart: unless-stopped
    user: "node"